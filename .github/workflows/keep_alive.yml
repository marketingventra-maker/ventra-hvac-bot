name: Keep Streamlit Alive
on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -A "Mozilla/5.0" -s https://marketingventra-maker-ventra-hvac-bot-app-dau5cp.streamlit.app/ > /dev/null
